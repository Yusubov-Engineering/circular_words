name: speech_impl
description: "Implementation details of the Speech recognition module"
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  speech_api:
  speech_to_text: ^7.3.0
  dependency_injection_api:
    git:
      url: https://github.com/Yusubov-Engineering/dependency_injection.git
      path: dependency_injection_api
      ref: v1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
