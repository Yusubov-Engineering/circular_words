name: assets
description: "Asset Storage module for whole app"
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  vector_graphics_compiler: ^1.3.0

flutter:
  assets:
    - rasters/
    - path: vectors/
      transformers:
        - package: vector_graphics_compiler